# Local environment — never commit secrets
.env
.env.local
.env.*.local

# Sanity write token — kept locally and on GitHub Pages, but never re-committed
# To update: edit app.config.js locally, then manually push that one file
app.config.js
_app.config.js

# macOS
.DS_Store

# Editor
.vscode/
*.swp
*.swo

# Node (Sanity Studio)
node_modules/
studio/node_modules/
